# Terraform local state + lock — contains secrets (Postgres password, EH SAS).
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.*.backup
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log

# Per-user vars
terraform.tfvars
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
